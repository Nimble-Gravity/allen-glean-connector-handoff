[pytest]
# The Custom Action API is a separate deployable with its own flat imports
# (from db import ..., from notifications_setup import ...). Run its tests from
# this directory:  cd allenco_custom_action && pytest
testpaths = tests
pythonpath = . ../src

markers =
    integration: tests that hit real external services and require env credentials
